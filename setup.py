"""
Legacy setup.py shim for Wilma.

Project metadata and dependencies are defined in pyproject.toml. This file
exists only for tools that still expect setup.py to be present.
"""

from setuptools import setup

setup()
